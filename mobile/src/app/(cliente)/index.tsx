// src/app/(cliente)/index.tsx
import PantallaPlaceholder from '../../components/PantallaPlaceholder';

export default function CatalogoScreen() {
  return <PantallaPlaceholder titulo="Catálogo" />;
}
