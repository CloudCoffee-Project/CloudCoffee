// src/app/(cliente)/resultado-pago.tsx
//
// No es tab (se navega desde carrito tras el checkout), por eso vive
// fuera de la barra de tabs pero dentro del mismo grupo (cliente).
import PantallaPlaceholder from '../../components/PantallaPlaceholder';

export default function ResultadoPagoScreen() {
  return <PantallaPlaceholder titulo="Resultado del Pago" />;
}
