// src/app/(cliente)/perfil.tsx
import PantallaPlaceholder from '../../components/PantallaPlaceholder';

export default function PerfilScreen() {
  return <PantallaPlaceholder titulo="Mi Perfil" />;
}
