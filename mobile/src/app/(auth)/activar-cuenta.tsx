// src/app/(auth)/activar-cuenta.tsx
//
// Nota: esta pantalla (activación de cuenta cajero, POST /auth/activation)
// no tiene issue explícita en el CSV del Sprint 1 -- se deja la ruta
// creada para no bloquear la navegación, pero falta agregarla al backlog.
import PantallaPlaceholder from '../../components/PantallaPlaceholder';

export default function ActivarCuentaScreen() {
  return <PantallaPlaceholder titulo="Activar Cuenta" />;
}
